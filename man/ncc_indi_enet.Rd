% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncc_indi_enet.R
\name{ncc_indi_enet}
\alias{ncc_indi_enet}
\title{Conditional Logistic Regression with Individual-level External Data and Elastic Net Penalty (CLR-Indi-ENet)}
\usage{
ncc_indi_enet(
  y_int,
  z_int,
  stratum_int,
  y_ext,
  z_ext,
  stratum_ext,
  etas,
  alpha = 1,
  lambda = NULL,
  nlambda = 100,
  lambda.min.ratio = NULL,
  lambda.early.stop = FALSE,
  tol = 1e-04,
  Mstop = 1000,
  max.total.iter = (Mstop * nlambda),
  group = NULL,
  group.multiplier = NULL,
  standardize = TRUE,
  nvar.max = NULL,
  group.max = NULL,
  stop.loss.ratio = 0.01,
  actSet = TRUE,
  actIter = Mstop,
  actGroupNum = NULL,
  actSetRemove = FALSE,
  returnX = FALSE,
  trace.lambda = FALSE,
  message = FALSE,
  ...
)
}
\arguments{
\item{y_int}{Numeric vector of binary outcomes for the internal dataset (0 = control, 1 = case).}

\item{z_int}{Numeric matrix of covariates for the internal dataset.}

\item{stratum_int}{Numeric or factor vector defining the internal matched sets. \strong{Required}.}

\item{y_ext}{Numeric vector of binary outcomes for the external dataset (0 = control, 1 = case).}

\item{z_ext}{Numeric matrix of covariates for the external dataset.}

\item{stratum_ext}{Numeric or factor vector defining the external matched sets. \strong{Required}.}

\item{etas}{Numeric vector of nonnegative external weights. \code{eta = 0} gives internal-only fit.}

\item{alpha}{The Elastic Net mixing parameter, with \eqn{0 < \alpha \le 1}.
\code{alpha = 1} is Lasso; \code{alpha} close to 0 approaches Ridge. Default \code{1}.}

\item{lambda}{Optional numeric vector of penalty parameters. If \code{NULL}, a path is
generated automatically for each \code{eta}.}

\item{nlambda}{Integer. Number of lambda values to generate. Default \code{100}.}

\item{lambda.min.ratio}{Numeric. Ratio of smallest to largest lambda. Default \code{NULL}
(determined automatically based on sample size vs. number of covariates).}

\item{lambda.early.stop}{Logical. If \code{TRUE}, stops the lambda path early if the
loss improvement is small. Default \code{FALSE}.}

\item{tol}{Convergence tolerance. Default \code{1e-4}.}

\item{Mstop}{Maximum coordinate descent iterations per lambda. Default \code{1000}.}

\item{max.total.iter}{Maximum total iterations across the entire lambda path.
Default \code{Mstop * nlambda}.}

\item{group}{Integer vector defining group membership for grouped penalties.
Default treats each variable as its own group.}

\item{group.multiplier}{Numeric vector of multiplicative factors for group penalties.}

\item{standardize}{Logical. If \code{TRUE}, \code{z} is standardized internally.
Coefficients are returned on the original scale. Default \code{TRUE}.}

\item{nvar.max}{Integer. Maximum number of active variables. Default \code{ncol(z_int)}.}

\item{group.max}{Integer. Maximum number of active groups.}

\item{stop.loss.ratio}{Numeric. Threshold for early stopping. Default \code{1e-2}.}

\item{actSet}{Logical. If \code{TRUE}, uses active-set strategy. Default \code{TRUE}.}

\item{actIter}{Integer. Iterations for active set refinement. Default \code{Mstop}.}

\item{actGroupNum}{Integer. Limit on active groups.}

\item{actSetRemove}{Logical. Whether to allow removal from active set. Default \code{FALSE}.}

\item{returnX}{Logical. If \code{TRUE}, returns the standardized design matrix. Default \code{FALSE}.}

\item{trace.lambda}{Logical. If \code{TRUE}, prints the lambda sequence progress. Default \code{FALSE}.}

\item{message}{Logical. If \code{TRUE}, shows a progress bar. Default \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link{cox_indi_enet}}.}
}
\value{
An object of class \code{"ncc_indi_enet"} and \code{"cox_indi_enet"}.
See \code{\link{cox_indi_enet}} for a description of the return components.
}
\description{
Fits a series of penalized Conditional Logistic Regression models for matched
case-control data that integrate external individual-level data via a composite
likelihood weight \code{etas}, while applying an Elastic Net penalty for
variable selection and regularization in high-dimensional settings.
}
\details{
This function maps the CLR problem to a Cox PH model with fixed event time
\eqn{T=1} and \eqn{\delta=y} for both internal and external datasets, then
calls \code{\link{cox_indi_enet}} as the core engine.

\itemize{
\item If \code{alpha = 1}, the penalty is Lasso.
\item If \code{alpha} is close to 0, the penalty approaches Ridge.
\item If \code{eta = 0}, external data is ignored and the model reduces to
a standard Elastic Net CLR on internal data only.
}
}
\seealso{
\code{\link{cox_indi_enet}}, \code{\link{ncc_indi}}
}
