% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cox.R
\name{cox}
\alias{cox}
\title{Estimate Cox Proportional Hazards Model Coefficients}
\usage{
cox(
  z,
  delta,
  time,
  stratum,
  ties = NULL,
  max_iter = 100,
  tol = 1e-07,
  comb_max = 1e+07
)
}
\arguments{
\item{z}{A matrix or data frame of covariates (n x p).}

\item{delta}{A binary event indicator vector (length n), where 1 = event and 0 = censored.}

\item{time}{A numeric vector of observed times (length n).}

\item{stratum}{A vector indicating strata for a stratified Cox model. If missing, all data is assumed to belong to a single stratum.}

\item{ties}{A character string specifying the method for tie handling. Options are "breslow" (default), "efron", or "exact".}

\item{max_iter}{Maximum number of Newton-Raphson iterations (default = 100).}

\item{tol}{Convergence tolerance for the Newton-Raphson update (default = 1e-7).}

\item{comb_max}{Maximum number of combinations allowed for the "exact" method (default = 1e7).}
}
\value{
A list containing:
\item{beta}{Estimated coefficient vector (length p).}
\item{loglik}{The log-partial likelihood at convergence.}
\item{...}{Additional outputs returned by the underlying Rcpp function.}
}
\description{
Estimates the coefficients of a Cox Proportional Hazards model using the Newton-Raphson method
implemented in C++ (Rcpp). It supports stratification, observation weights, and different
tie-handling methods (Breslow, Efron, Exact).
}
\examples{
\donttest{
data(ExampleData_lowdim)
train_dat_lowdim <- ExampleData_lowdim$train
train_dat_lowdim$time <- round(train_dat_lowdim$time, 2)  #make ties

fit <- cox(z = train_dat_lowdim$z,
           delta = train_dat_lowdim$status,
           time = train_dat_lowdim$time)

# Breslow method for tied data
fit_Breslow <- cox(z = train_dat_lowdim$z,
                   delta = train_dat_lowdim$status,
                   time = train_dat_lowdim$time,
                   ties = "breslow")
}
}
\keyword{internal}
