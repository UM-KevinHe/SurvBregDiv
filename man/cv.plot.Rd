% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.plot.R
\name{cv.plot}
\alias{cv.plot}
\title{Plot Cross-Validation Results vs Eta (Biometrics-style)}
\usage{
cv.plot(object, line_color = "#7570B3", baseline_color = "#1B9E77", ...)
}
\arguments{
\item{object}{A fitted cross-validation result of class \code{"cv.coxkl"},
\code{"cv.coxkl_ridge"}, or \code{"cv.coxkl_enet"}.}

\item{line_color}{Color for the CV performance curve. Default is \code{"#7570B3"}.}

\item{baseline_color}{Color for the external baseline line. Default is \code{"#1B9E77"}.}

\item{...}{Additional arguments (currently ignored).}
}
\value{
A \code{ggplot} object showing cross-validation performance versus \code{eta}.
}
\description{
Plots cross-validation performance across eta values for
\code{cv.coxkl}, \code{cv.coxkl_ridge}, or \code{cv.coxkl_enet} objects
in the Biometrics figure style. It displays the CV performance curve,
a baseline reference at eta=0, and marks the optimal eta.
}
\examples{
data(ExampleData_lowdim)
train_dat_lowdim <- ExampleData_lowdim$train
test_dat_lowdim <- ExampleData_lowdim$test
beta_external_lowdim <- ExampleData_lowdim$beta_external_good

eta_list <- generate_eta(method = "exponential", n = 50, max_eta = 30)
cv.coxkl_est <- cv.coxkl(z = train_dat_lowdim$z,
                         delta = train_dat_lowdim$status,
                         time = train_dat_lowdim$time,
                         beta = beta_external_lowdim,
                         etas = eta_list,
                         criteria = "V&VH",
                         seed = 1)
cv.plot(cv.coxkl_est)

}
